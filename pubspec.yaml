name: v12_like
description: "Train yourself to write blazingly fast."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.5 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.1
  http: ^1.2.0
  json_annotation: ^4.8.1

dev_dependencies:
  blackfoot_flutter_lint: ^2.0.0
  build_runner: ^2.4.8
  flutter_test:
    sdk: flutter
  freezed: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
